pipeline {
  agent {
    docker { image 'node:latest' }
  }
  stages {
    stage('Install') {
      steps {
            sh 'cd app && npm cache clean --force && npm install'
        }
    }

    // stage('Test') {
    //   parallel {
    //     stage('Static code analysis') {
    //         steps { sh 'npm run-script lint' }
    //     }
    //     stage('Unit tests') {
    //         steps { sh 'npm run-script test' }
    //     }
    //   }
    // }

    stage('Build') {
      steps {
            echo 'Start Building' 
            sh 'ng --version'
            sh 'cd app && npm run ng build --prod' 
        }
    }
  }
}